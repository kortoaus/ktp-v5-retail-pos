# CLAUDE.md

Compatibility shim for Claude Code.

The canonical repository instructions now live in `AGENTS.md`. Product,
architecture, routes, permissions, hardware, and command references live in
`README.md`. Sale/payment/refund/repay/shift/cloud-sync decisions live in
`docs/sale-domain.md`.

Do not duplicate those files here; update the canonical docs instead.
